<%= form_with scope: :session, url: sessions_url do |f| %>

  <%= f.label :email %>
  <%= f.email_field :email %>

  <%= f.label :password %>
  <%= f.text_field :password %>

  <%= f.submit "Log In"%>
<% end %>
